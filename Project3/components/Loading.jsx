import { View } from 'react-native'

export default function Loading() {
    return (
        <View style={{
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center'
        }}>Loading ...</View>
    )
}
